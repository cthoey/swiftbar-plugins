# iSMC Helper

This directory is populated locally by `scripts/install-ismc.sh`.

The SwiftBar temperature plugin prefers `vendor/ismc/iSMC` and falls back to `iSMC` or `ismc` on `PATH` if you install the helper another way.
